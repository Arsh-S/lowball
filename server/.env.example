# Vapi — vapi.ai dashboard → API key; buy/import a phone number for its ID
VAPI_API_KEY=
VAPI_PHONE_NUMBER_ID=
# Outbound calling master switch — calls are DISABLED unless this is "true"
CALLS_ENABLED=
# Demo safety lock: the ONLY number the agent will ever dial (E.164, e.g. +15551234567)
DEMO_CALL_NUMBER=
# Apify — token + the FBM/dealer-listing actor id (Gabe)
APIFY_TOKEN=
APIFY_ACTOR_ID=
# OpenAI — hackathon credits
OPENAI_API_KEY=
# ngrok static domain, e.g. https://lowball.ngrok.app (no trailing slash)
PUBLIC_DOMAIN=
PORT=8081
# Python scraper HTTP service (scraping/serve.py) — run: python3 -m uvicorn scraping.serve:app --port 8090
SCRAPER_URL=http://localhost:8090
